*.o
*~
*.roh
*.gz
*.kde
*.dvi
*.log
*.bbl
*.toc
*.aux
*.blg
.DS_Store

# generated by the Makefile
# src/garlic was tracked, so every rebuild showed up as a modified binary and a
# commit could carry one silently.  The distributed builds are the ones under
# bin/; this is just the local one.
src/garlic
src/garlic.exe
src/*.d
src/garlic-version.h
src/garlic-version.h.tmp
src/README.docs.tmp
